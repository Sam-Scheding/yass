{
  "name": "yass",
  "version": "1.0.0",
  "description": "An atomic CSS library",
  "main": "src/index.scss",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "node-sass -w src/index.scss -o css/"
  },
  "author": "Sam Scheding",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^7.0.1"
  }
}
